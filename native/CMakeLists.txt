cmake_minimum_required(VERSION 3.20)
project(VoraNative VERSION 0.2.0 LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
if(MSVC)
  add_compile_options(/W4 /utf-8 /EHsc)
endif()
include_directories(include vendor)
add_executable(vora src/main.cpp)
if(WIN32)
  target_link_libraries(vora PRIVATE winhttp)
endif()
set_target_properties(vora PROPERTIES RUNTIME_OUTPUT_DIRECTORY_RELEASE "${CMAKE_CURRENT_SOURCE_DIR}/dist")
enable_testing()
add_executable(parser_tests tests/parser_tests.cpp)
add_executable(runtime_tests tests/runtime_tests.cpp)
add_executable(validation_tests tests/validation_tests.cpp)
add_test(NAME parser COMMAND parser_tests)
add_test(NAME runtime COMMAND runtime_tests)
add_test(NAME validation COMMAND validation_tests)
